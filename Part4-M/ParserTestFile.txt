CREATE TABLE a (name VARCHAR(20), kind VARCHAR(8), years INTEGER) PRIMARY KEY (name, kind);
INSERT INTO a VALUES FROM ("Joe", "cat", 4);
INSERT INTO a VALUES FROM ("Spot", "dog", 10);
CREATE TABLE b (name VARCHAR(20), kind VARCHAR(8), years INTEGER) PRIMARY KEY (name, kind);
INSERT INTO b VALUES FROM ("Snoopy", "dog", 3);
CREATE TABLE c (name VARCHAR(20), kind VARCHAR(8), years INTEGER) PRIMARY KEY (name, kind);
INSERT INTO c VALUES FROM ("Tweety", "bird", 1);
CREATE TABLE d (name VARCHAR(20), kind VARCHAR(8), years INTEGER) PRIMARY KEY (name, kind);
INSERT INTO d VALUES FROM ("Joe", "bird", 2);
e <- (a + b) + c;
WRITE e;
SHOW e;
f <- rename (name, NAME) a;
SHOW f;
g <- project (name) e;
SHOW g;
EXIT;
